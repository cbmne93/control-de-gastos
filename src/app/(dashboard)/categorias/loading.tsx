import { PageLoading } from "@/components/shared/PageLoading";

export default function CategoriasLoading() {
    return (
        <PageLoading
            title="Cargando categorías"
            description="Preparando categorías de ingresos y gastos."
        />
    );
}