import { PageLoading } from "@/components/shared/PageLoading";

export default function ReportesLoading() {
    return (
        <PageLoading
            title="Cargando reportes"
            description="Preparando tus resúmenes, filtros y exportaciones."
        />
    );
}