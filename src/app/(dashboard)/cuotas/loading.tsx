import { PageLoading } from "@/components/shared/PageLoading";

export default function CuotasLoading() {
    return (
        <PageLoading
            title="Cargando cuotas"
            description="Preparando vencimientos, estados y compromisos pendientes."
        />
    );
}