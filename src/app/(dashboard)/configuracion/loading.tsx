import { PageLoading } from "@/components/shared/PageLoading";

export default function ConfiguracionLoading() {
    return (
        <PageLoading
            title="Cargando configuración"
            description="Preparando preferencias, perfil y apariencia."
        />
    );
}